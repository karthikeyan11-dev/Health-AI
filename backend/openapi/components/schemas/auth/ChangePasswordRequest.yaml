type: object
required:
  - currentPassword
  - newPassword
properties:
  currentPassword:
    type: string
    format: password
    example: "OldP@ssw0rd123!"
  newPassword:
    type: string
    format: password
    minLength: 8
    example: "NewP@ssw0rd123!"
