type: object
required:
  - email
  - password
properties:
  email:
    type: string
    format: email
    example: "user@healthai.org"
  password:
    type: string
    format: password
    example: "P@ssw0rd123!"
