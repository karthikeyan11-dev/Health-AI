type: object
required:
  - success
  - message
  - data
  - meta
properties:
  success:
    type: boolean
    example: true
  message:
    type: string
    example: "Patient health overview retrieved successfully"
  data:
    $ref: './PatientOverviewData.yaml'
  meta:
    $ref: '../common/Metadata.yaml'
